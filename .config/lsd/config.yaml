# == Classic ==
classic: false

# == Blocks ==
blocks:
  - permission
  - size
  - user
  - date
  - name

# == Color ==
color:
  when: auto
  theme: custom

# == Date ==
date: date

# == Dereference ==
dereference: false

# == Icons ==
icons:
  when: auto
  theme: fancy
  separator: " "

# == Indicators ==
indicators: false

# == Layout ==
layout: grid

# == Recursion ==
recursion:
  enabled: false

# == Size ==
size: short

# == Sorting ==
sorting:
  column: name
  reverse: false
  dir-grouping: none

# == No Symlink ==
no-symlink: false

# == Total size ==
total-size: false

# == Hyperlink ==
hyperlink: auto

# == Symlink arrow ==
symlink-arrow: 

# == Header ==
header: false

# == Literal ==
literal: false
